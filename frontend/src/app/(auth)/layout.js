// ============================================
// 📄 app/(auth)/layout.js - Auth Layout (No Header/Footer)
// ============================================

export default function AuthLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}